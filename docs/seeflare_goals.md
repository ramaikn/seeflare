1. I want to add an API feature to this project, Seeflare, a Counterscale fork, so that all analytics data can be retrieved when users want to build a custom dashboard with complete data, matching the original dashboard 1:1.
2. Add 120d, 1y, 3y, 5y, and all time range features to the dashboard. These should work using D1 plus a cached API layer. Use Cron Triggers to also back up raw data to R2, using Arrow according to the existing Counterscale system, and purge the cache so the displayed cached data resets daily, since D1 data does not change within the same day. Research whether using the Workers Cache API or KV is the better option. The goal is to reduce long term read limits and keep the system efficient.
3. Design a very smart D1 aggregation system and database management strategy. The goal is to display complete dashboard data with no meaningful difference from using WAE 1:1. Everything currently shown in the dashboard must remain available, and no existing dashboard features should be removed. At the same time, the system must intelligently prevent D1 from growing too large over time.
4. The focus must stay aligned with Counterscale’s philosophy: set it up once and forget it. Research carefully online whether this is technically possible or not. Create an implementation_plan.md file that includes the implementation plan, advantages, disadvantages, limitations, technical risks, and recommended architecture.